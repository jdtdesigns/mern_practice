type Task {
  _id: String
  text: String
  username: String
  createdAt: String
  updatedAt: String
}

type Query {
  getTasks: [Task]
  getTask(id: String): Task
}

type Mutation {
  addTask(username: String!, text: String!): Task
}